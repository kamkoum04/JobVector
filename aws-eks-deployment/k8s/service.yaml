apiVersion: v1
kind: Service
metadata:
  name: cv-ai-matcher-service
  labels:
    app: cv-ai-matcher
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: cv-ai-matcher